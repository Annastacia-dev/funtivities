
<div class="text-white min-h-screen bg-image bg-cover bg-fixed bg-center bg-no-repeat relative" style="background-image: url(<%= @background_image %> )">

  <div class="min-h-screen bg-gradient-to-r from-black to-black/90 px-10 py-6 ">

  <%= render 'components/navigation' %>


  <div class="flex flex-col gap-5 mt-10">
    <h1 class="text-xl font-black font-montserrat">Sign up</h1>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "grid sm:grid-cols-2 grid-cols-1 gap-6"}) do |f| %>

      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="sm:grid sm:grid-cols-2 flex flex-col gap-6">
        <div class="flex flex-col gap-1">
          <%= f.label :first_name, class: 'text-sm font-bold ml-1' %>
          <%= f.text_field :first_name, class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', placeholder: 'e.g "John"', required: true %>
        </div>
        <div class="flex flex-col gap-1">
          <%= f.label :last_name, class: 'text-sm font-bold ml-1' %>
          <%= f.text_field :last_name, class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', placeholder: 'e.g "Doe"', required: true %>
        </div>
        <div class="flex flex-col gap-1">
          <%= f.label :email, class: 'text-sm font-bold ml-1' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', placeholder: 'e.g "johndoe@email.com"', required: true   %>
        </div>
        <div class="flex flex-col gap-1">
          <%= f.label :phone_number, class: 'text-sm font-bold ml-1' %>
          <%= f.email_field :phone_number, autofocus: true, class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', placeholder: 'e.g "07xxxxxxxx"', required: true   %>
        </div>

        <div class="flex flex-col gap-1 relative">
          <div class='flex gap-1'>
            <%= f.label :password, class:'text-sm font-bold ml-1'  %>
            <% if @minimum_password_length %>
            <em class="text-sm">(<%= @minimum_password_length %> characters minimum)</em>
            <% end %>
          </div>
          <%= f.password_field :password, autocomplete: "new-password", class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', required: true, id: 'password-field' %>
          <i class="fa fa-eye-slash absolute right-4 top-10 text-sm cursor-pointer" id="show-password"></i>
          <i class="fa fa-eye absolute right-4 top-10 text-sm cursor-pointer" id="hide-password" style="display: none"></i>
        </div>

        <div class="flex flex-col gap-1 relative">
          <%= f.label :password_confirmation, class:'text-sm font-bold ml-1' %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'border-none bg-gray-500/20 text-white px-4 py-3 rounded-lg placeholder:text-sm', required: true, id: 'password-confirmation-field' %>
          <i class="fa fa-eye-slash absolute right-4 top-10 text-sm cursor-pointer" id="show-password-confirmation"></i>
          <i class="fa fa-eye absolute right-4 top-10 text-sm cursor-pointer" id="hide-password-confirmation" style="display: none"></i>
        </div>

        <div class="flex items-center gap-2 my-4">
          <%= f.check_box :terms_and_conditions_and_privacy_policy, class: 'border-none bg-gray-500/20  h-8 w-8 rounded-lg', required: true %>
          <%= f.label :terms_and_conditions_and_privacy_policy, class: 'text-sm font-bold ml-1' %>
        </div>

        <div class="actions">
          <%= f.submit "Sign up", class: 'bg-secondary hover:bg-primary text-white px-6 py-3 rounded-lg font-bold cursor-pointer w-full' %>
        </div>
      </div>
    <% end %>
  </div>

  <%= render "devise/shared/links" %>

  </div>
</div>
